# ShareHub Environment Configuration
# Copy this file to .env.local and update with your values

# Set to 'development' to use mock data (no AWS needed)
# Set to 'production' to use real AWS services
VITE_APP_ENVIRONMENT=development

# AWS Configuration (leave empty for development mode)
VITE_AWS_REGION=us-east-1
VITE_AWS_USER_POOL_ID=
VITE_AWS_USER_POOL_WEB_CLIENT_ID=
VITE_AWS_IDENTITY_POOL_ID=
VITE_AWS_S3_BUCKET=
VITE_AWS_API_GATEWAY_URL=
VITE_AWS_SNS_TOPIC_ARN=

# App Configuration
VITE_APP_NAME=ShareHub

# ====================
# INSTRUCTIONS:
# ====================
# 1. Copy this file: cp env.example .env.local
# 2. For development (no AWS): Keep VITE_APP_ENVIRONMENT=development
# 3. For production (with AWS): 
#    - Set VITE_APP_ENVIRONMENT=production
#    - Fill in AWS credentials from AWS Console
# ====================


